This directory is reserved for bibliography fixtures if the repository is later
extended to test per-chapter reference files. The current root fixture uses
references.bib so it matches the repository's current report configuration.
